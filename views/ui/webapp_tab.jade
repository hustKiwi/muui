extends ../core/webapp_base

append vars
    - var uiName = 'tab'

block content
    #tab-placeholder
    .muui-tab-panel.rec-panel.on.
        推荐
    .muui-tab-panel.pop-panel.
        流行

block css
    :stylus
        .muui-tab-panel
            padding-top: 50px
            text-align: center
            font-size: 24px

block js
    :coffee
        require [
            'muui/webapp_tab/index'
        ], (WebAppTab) ->
            new WebAppTab({
                el: '#tab-placeholder'
                dataSource: '/datasource/tab'
                dataFilter: (r) ->
                    r.cur = '.rec-panel'
                    r.items = r.items.slice(0, 2)
                    r
            })
