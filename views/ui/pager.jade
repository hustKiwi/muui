extends ../core/base

append vars
    - var ui_name = 'pager'

block content
    #panel.
        第1页
    #pager-placeholder

block css
    :css
        .muui-tab-panel {
            padding-top: 50px;
            text-align: center;
            font-size: 24px;
        }

block js
    :coffee
        require [
            'muui/ui/pager'
        ], (UI) ->
            pager = new UI({
                el: '#pager-placeholder'
                datasource: '/datasource/tab'
            })

            console.log pager.build(29, 281, 10)
