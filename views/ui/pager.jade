extends ../core/base

append vars
    - var ui_name = 'pager'

block content
    #panel.
        第1页
    #pager-placeholder

block css
    :css
        .muui-tab-panel {
            padding-top: 50px;
            text-align: center;
            font-size: 24px;
        }

block js
    :coffee
        require [
            'muui/ui/pager'
        ], (Pager) ->
            pager = new Pager({
                el: '#pager-placeholder'
                datasource: '/datasource/tab'
                render_args:
                    cur: 10
                    total: 199
                    size: 10
                    length: 8
                after_render: (r) ->
                    console.log r
            })
